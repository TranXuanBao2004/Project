@model IEnumerable<Brand>
@foreach(var item in Model)
{
	<ul class="nav nav-pills nav-stacked">
		<li><a asp-action="Index" asp-controller="Brand" asp-route-Slug="@item.Slug"> <span class="pull-right">@item.Products.Count</span>@item.Name</a></li>
	</ul>
}